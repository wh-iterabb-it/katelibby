const kate = require('../app.js').default;

kate.init();

process.on('message', msg => {
  if (msg === 'shutdown') {
    process.exit(0);
  }
});

process.on('SIGTERM', () => {
  process.exit(1);
});
